import {
  spotifyHelper
} from 'wiki-search/helpers/spotify-helper';

module('SpotifyHelperHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = spotifyHelper(42);
  ok(result);
});
